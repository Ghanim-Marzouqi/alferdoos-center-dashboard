import { COLLECTIONS } from "../../config/constants";

const state = {
  registered_students: [],
  student: {}
};

const getters = {};

const actions = {};

const mutations = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
};
